<!DOCTYPE html>
<html>
<head>
    <title>智能教务系统 Pro</title>
    <meta charset="utf-8">
    <style>
        /* 全局基础设置 */
        body { 
            font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, Roboto, sans-serif; 
            margin: 0;
            background-color: #f1f5f9;
            color: #1e293b;
            display: flex;
            height: 100vh; /* 铺满整个屏幕高度 */
            overflow: hidden;
        }

        /* ================= 左侧导航栏 (Sidebar) ================= */
        .sidebar {
            width: 260px;
            background-color: #1e293b;
            color: #f8fafc;
            display: flex;
            flex-direction: column;
            padding: 20px 0;
            box-shadow: 2px 0 10px rgba(0,0,0,0.1);
            z-index: 10;
        }

        .sidebar-header {
            padding: 0 20px 20px 20px;
            border-bottom: 1px solid #334155;
            margin-bottom: 20px;
        }
        
        .sidebar-header h2 { margin: 0; font-size: 20px; display: flex; align-items: center; gap: 10px;}

        .menu-list {
            list-style: none;
            padding: 0;
            margin: 0;
            flex: 1;
        }

        .menu-item {
            padding: 15px 25px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 500;
            transition: all 0.2s ease;
            color: #94a3b8;
        }

        .menu-item:hover {
            background-color: #334155;
            color: #ffffff;
        }

        /* 激活状态的菜单项 */
        .menu-item.active {
            background-color: #2563eb;
            color: #ffffff;
            border-left: 4px solid #60a5fa;
        }

        /* ================= 右侧主内容区 (Main Content) ================= */
        .main-area {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 30px;
            background-image: radial-gradient(#e2e8f0 1px, transparent 1px);
            background-size: 20px 20px;
        }

        .chat-container {
            flex: 1;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            border: 1px solid #e2e8f0;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            max-width: 1000px;
            margin: 0 auto;
            width: 100%;
        }

        /* 聊天区域 */
        #chatBox {
            flex: 1;
            overflow-y: auto;
            padding: 25px;
            display: flex;
            flex-direction: column;
            gap: 18px;
            scroll-behavior: smooth;
        }

        #chatBox::-webkit-scrollbar { width: 6px; }
        #chatBox::-webkit-scrollbar-thumb { background-color: #cbd5e1; border-radius: 10px; }

        /* 聊天气泡 */
        .msg-bubble {
            max-width: 80%;
            padding: 14px 18px;
            border-radius: 16px;
            font-size: 15px;
            line-height: 1.6;
            word-wrap: break-word;
            animation: fadeInUp 0.3s ease;
        }
        .msg-user {
            align-self: flex-end;
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: white;
            border-bottom-right-radius: 4px;
        }
        .msg-ai {
            align-self: flex-start;
            background: #f1f5f9;
            color: #334155;
            border-bottom-left-radius: 4px;
        }
        .msg-ai.success { border-left: 4px solid #10b981; }
        .msg-ai.danger { border-left: 4px solid #ef4444; background: #fef2f2; color: #991b1b; }

        /* 输入区 */
        .chat-input-area {
            display: flex;
            padding: 20px;
            background: #ffffff;
            border-top: 1px solid #f1f5f9;
            gap: 15px;
        }
        .chat-input-area input {
            flex: 1;
            padding: 14px 20px;
            border-radius: 12px;
            border: 1px solid #cbd5e1;
            font-size: 16px;
            outline: none;
            background: #f8fafc;
            transition: border 0.3s;
        }
        .chat-input-area input:focus { border-color: #3b82f6; background: #ffffff; }

        .action-btn {
            padding: 0 30px; 
            font-size: 16px; 
            font-weight: bold;
            cursor: pointer;
            background: #2563eb;
            color: white;
            border: none;
            border-radius: 12px;
            transition: all 0.2s;
        }
        .action-btn:hover { background: #1d4ed8; transform: translateY(-1px); }
        .action-btn:disabled { background: #94a3b8; cursor: not-allowed; }

        @keyframes fadeInUp { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body>

    <aside class="sidebar">
        <div class="sidebar-header">
            <h2>🚀 教务系统 Pro</h2>
        </div>
        <ul class="menu-list">
            <li class="menu-item active">💬 AI 对话大厅</li>
            <li class="menu-item" onclick="quickSend('帮我生成我全部成绩报表')">📊 一键生成报表</li>
            <li class="menu-item" onclick="quickSend('查询不及格的学科')">⚠️ 异常成绩预警</li>
            <li class="menu-item" style="opacity: 0.5; cursor: not-allowed;">⚙️ 系统设置 (开发中)</li>
        </ul>
    </aside>

    <main class="main-area">
        <div class="chat-container">
            <div id="chatBox">
                <div class="msg-bubble msg-ai">
                    👋 欢迎进入系统！我是你的专属 AI 教务助理。<br>
                    左侧栏是功能快捷键，你也可以直接在这里输入指令（例如：录入物理 85分）。
                </div>
            </div>
            <form id="chatForm" class="chat-input-area">
                <input type="text" id="chatInput" placeholder="输入你想办理的业务..." autocomplete="off">
                <button type="submit" class="action-btn" id="chatBtn">发送</button>
            </form>
        </div>
    </main>

<script>
    // ⚠️ 注意：这里要换成你 n8n 的 【Production (生产) Webhook 网址】！
    // 它通常不带 "-test" 字样，例如：http://localhost:5678/webhook/v1/ai-orchestrator
    const WEBHOOK_URL = 'http://localhost:5678/webhook/v1/ai-orchestrator'; 

    const mySessionId = "session_" + Math.random().toString(36).substring(2, 9);
    const chatBox = document.getElementById('chatBox');

    function addChatMessage(sender, text, status = '') {
        const msgDiv = document.createElement('div');
        msgDiv.className = `msg-bubble msg-${sender} ${status}`;
        msgDiv.innerHTML = text.replace(/\n/g, '<br>');
        chatBox.appendChild(msgDiv);
        chatBox.scrollTop = chatBox.scrollHeight;
        return msgDiv; 
    }

    // 左侧边栏的快捷发送功能
    function quickSend(message) {
        document.getElementById('chatInput').value = message;
        document.getElementById('chatBtn').click(); // 自动模拟点击发送按钮
    }

    document.getElementById('chatForm').onsubmit = async (e) => {
        e.preventDefault();
        const chatInput = document.getElementById('chatInput');
        const userText = chatInput.value.trim();
        const chatBtn = document.getElementById('chatBtn');
        if (!userText) return;

        addChatMessage('user', userText);
        chatInput.value = ''; 
        chatBtn.disabled = true;
        const loadingMsg = addChatMessage('ai', '⏳ 正在云端处理中...');

        try {
            const response = await fetch(WEBHOOK_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ body: userText, sessionId: mySessionId })
            });
            const data = await response.json();
            
            const finalReport = data.report_text || (typeof data === 'string' ? data : "✅ 已处理。");
            
            let status = (finalReport.includes("⚠️") || finalReport.includes("不及格")) ? 'danger' : '';
            if (finalReport.includes("✅") || finalReport.includes("成功")) status = 'success';

            loadingMsg.remove();
            addChatMessage('ai', finalReport, status);
        } catch (err) {
            loadingMsg.remove();
            addChatMessage('ai', "❌ 与 n8n 连接失败，请检查你的 n8n Workflow 是否已点击右上角的 Active 激活开关？", 'danger');
        } finally {
            chatBtn.disabled = false;
        }
    };
</script>   
</body>
</html>